rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /posts/{document=**} {
      allow read, write: if true;
    }
    match /feedback_posts/{document=**} {
      allow read, write: if true;
    }
  }
}
